// I had to put all library definitions in defs.ts because TS will not ignore
// any .d.ts file defined anywhere, meaning that all these definitions end up
// being the cannonical definitions used by the compiler.